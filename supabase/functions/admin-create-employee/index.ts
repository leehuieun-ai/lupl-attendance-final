import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{...corsHeaders,"Content-Type":"application/json"}})}
function internalEmail(no:string){return `${no.trim().toLowerCase()}@lupl.local`}
function initialPassword(phone:string){const digits=String(phone||"").replace(/\D/g,"");const last4=digits.slice(-4);if(last4.length!==4)throw new Error("휴대폰 번호 뒷자리 4자리를 확인할 수 없습니다.");return `lupl${last4}`}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});try{const authHeader=req.headers.get("Authorization");if(!authHeader)return json({error:"로그인이 필요합니다."},401);const supabaseUrl=Deno.env.get("SUPABASE_URL");const anonKey=Deno.env.get("SUPABASE_ANON_KEY");const serviceRoleKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!supabaseUrl||!anonKey||!serviceRoleKey)return json({error:"Supabase Secret 설정이 부족합니다."},500);const userClient=createClient(supabaseUrl,anonKey,{global:{headers:{Authorization:authHeader}}});const {data:{user},error:userError}=await userClient.auth.getUser();if(userError||!user)return json({error:"로그인 정보를 확인할 수 없습니다."},401);const adminClient=createClient(supabaseUrl,serviceRoleKey);const {data:adminEmployee}=await adminClient.from("employees").select("id, role, is_active").eq("user_id",user.id).single();if(!adminEmployee||adminEmployee.role!=="admin"||!adminEmployee.is_active)return json({error:"관리자만 직원 계정을 생성할 수 있습니다."},403);const body=await req.json();const name=String(body.name??"").trim();const employeeNo=String(body.employee_no??"").trim();const phone=String(body.phone??"").trim();const joinedAt=String(body.joined_at??new Date().toISOString().slice(0,10));const role=body.role==="admin"?"admin":"employee";const deviceLimit=Math.min(3,Math.max(1,Number(body.device_limit??3)));if(!name||!employeeNo||!phone)return json({error:"이름, 사번, 휴대폰 번호는 필수입니다."},400);const email=internalEmail(employeeNo);const password=initialPassword(phone);const {data:created,error:createError}=await adminClient.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{name,employee_no:employeeNo,phone}});if(createError||!created.user)return json({error:createError?.message??"직원 계정 생성 실패"},400);const {error:upsertError}=await adminClient.from("employees").upsert({user_id:created.user.id,employee_no:employeeNo,name,phone,internal_email:email,role,device_limit:deviceLimit,joined_at:joinedAt,employment_status:"active",is_active:true},{onConflict:"employee_no"});if(upsertError)return json({error:upsertError.message},400);return json({ok:true,employee_no:employeeNo,initial_password:password})}catch(error){return json({error:String(error)},500)}});
