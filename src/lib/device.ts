export type DeviceInfo={localDeviceId:string;userAgent:string;platform:string;language:string;timezone:string;screen:string;hardwareConcurrency:number|null};
function getOrCreateLocalDeviceId(){const k="lupl_device_id_v2";const s=localStorage.getItem(k);if(s)return s;const id=crypto.randomUUID();localStorage.setItem(k,id);return id}
async function sha256(input:string){const bytes=new TextEncoder().encode(input);const hash=await crypto.subtle.digest("SHA-256",bytes);return Array.from(new Uint8Array(hash)).map(b=>b.toString(16).padStart(2,"0")).join("")}
export async function getDeviceFingerprint(){const deviceInfo:DeviceInfo={localDeviceId:getOrCreateLocalDeviceId(),userAgent:navigator.userAgent,platform:navigator.platform,language:navigator.language,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone,screen:`${screen.width}x${screen.height}x${screen.colorDepth}`,hardwareConcurrency:navigator.hardwareConcurrency??null};return{deviceInfo,fingerprintHash:await sha256(JSON.stringify(deviceInfo))}}
